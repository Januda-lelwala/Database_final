# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MySQL - KandyPack)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=kandypack
DB_USER=root
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# CORS
# Comma-separated origins allowed for browser clients
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001
